// Playground - noun: a place where people can play

import UIKit

/*
let htmlBody = NSString.stringWithContentsOfFile("/Users/tos/Desktop/BB.txt") as NSString

var error: NSError? = nil

let bitrateMask = ">\\[(.+?) kbps\\]</span>";
let bitrateRegex = NSRegularExpression.regularExpressionWithPattern(bitrateMask, options: NSRegularExpressionOptions.CaseInsensitive, error: &error)
let bitrateMatches = bitrateRegex.numberOfMatchesInString(htmlBody, options: NSMatchingOptions.ReportCompletion, range: NSMakeRange(0, htmlBody.length))
let bitrateMatchRange = bitrateRegex.rangeOfFirstMatchInString(htmlBody, options: NSMatchingOptions.ReportCompletion, range: NSMakeRange(0, htmlBody.length))
let bitrateMatch = htmlBody.substringWithRange(bitrateMatchRange)

let mp3Mask = "<a[^>]*href=\"(.+?).mp3\"";
let mp3Regex = NSRegularExpression.regularExpressionWithPattern(mp3Mask, options: NSRegularExpressionOptions.CaseInsensitive, error: &error)
let mp3Matches = mp3Regex.numberOfMatchesInString(htmlBody, options: NSMatchingOptions.ReportCompletion, range: NSMakeRange(0, htmlBody.length))
let mp3MatchRange = mp3Regex.rangeOfFirstMatchInString(htmlBody, options: NSMatchingOptions.ReportCompletion, range: NSMakeRange(0, htmlBody.length))
let mp3Match = htmlBody.substringWithRange(mp3MatchRange)

*/